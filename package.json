{
  "name": "csv-point-in-polygon-webapp",
  "description": "Web app which assigns properties from a GeoJSON polygon to a CSV of points based on a point in polygon test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "file-saver": "^1.3.3",
    "papaparse": "^4.3.6",
    "which-polygon": "^2.2.0"
  },
  "devDependencies": {
    "browserify": "^14.5.0"
  },
  "scripts": {
    "build": "./node_modules/.bin/browserify index.js > index.min.js"
  }
}
